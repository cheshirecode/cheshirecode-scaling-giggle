import type { ProductType } from '@/types/api';

/**
 * Default product types to display on home page
 * Per wireframe: Show best FIXED and best VARIABLE products
 */
export const DEFAULT_PRODUCT_TYPES_TO_DISPLAY: ProductType[] = ['FIXED', 'VARIABLE'];
